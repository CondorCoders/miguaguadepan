---

---

<button id="clear" type="button" class="gallery-item" title="Eliminar elemento">
  <img src="/guagua/icons/prohibition.svg" alt="Eliminar" />
</button>

<style>
  .gallery-item {
    padding: 0.25rem;
    background: rgba(255, 255, 255, 0.6);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(254, 202, 202, 0.3);
    cursor: pointer;
    aspect-ratio: 1 / 1;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 1rem;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .gallery-item:hover {
    background: rgba(254, 202, 202, 0.7);
    border-color: rgba(248, 113, 113, 0.5);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(248, 113, 113, 0.25);
  }

  .gallery-item:focus-visible {
    outline: 3px solid #f87171;
    outline-offset: 2px;
  }

  .gallery-item img {
    width: 50%;
    height: 50%;
    object-fit: contain;
    display: block;
  }
</style>
